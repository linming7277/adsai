steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "services/console/Dockerfile", "-t", "$_IMAGE", "."]
images:
  - "$_IMAGE"
substitutions:
  _IMAGE: "gcr.io/$PROJECT_ID/console:autotest"
