# AutoAds 前端开发快速开始指南

> 🚀 立即开始前端开发的完整指南
>
> 更新时间: 2024-10-03
> 文档完整度: ✅ 95%

---

## 📚 文档导航

| 文档 | 用途 | 状态 |
|------|------|------|
| **00-快速开始指南.md** | 总览导航（本文档） | ✅ 完成 |
| **01-页面设计方案.md** | 4个核心页面UI设计 | ✅ 完成 |
| **02-技术实现方案.md** | 技术栈和组件复用 | ✅ 完成 |
| **03-数据库表结构.md** | PostgreSQL + Firestore | ✅ 完成 |
| **04-交互原型设计.md** | 7个核心流程 | ✅ 完成 |
| **05-API接口设计.md** | 37个API端点 | ✅ 完成 |
| **06-前端实施评估报告.md** | 可行性分析 | ✅ 完成 |
| **07-前端环境配置指南.md** | 环境配置详解 | ✅ 完成 |
| **08-配置补充说明.md** | 深入技术解答 | ✅ 完成 |
| **09-配置完成总结.md** | 配置任务总结 | ✅ 完成 |
| **10-路由优化实施指南.md** | 路由结构优化 | ⚠️ 已废弃 |
| **11-路由优化完成说明.md** | 路由优化说明 | ⚠️ 已废弃 |
| **12-路由架构澄清说明.md** | 路由架构澄清 | ✅ 完成 |

---

## ✅ 已确认的关键配置

### 1. API Gateway

| 环境 | 域名 |
|------|------|
| **Preview** | `https://autoads-gw-preview-885pd7lz.an.gateway.dev/api/v1` |
| **Production** | `https://autoads-gw-885pd7lz.an.gateway.dev/api/v1` |

### 2. 前端域名

| 环境 | 域名 |
|------|------|
| **Preview** | `https://www.urlchecker.dev` |
| **Production** | `https://www.autoads.dev` |

### 3. Firebase 项目

- Project ID: `gen-lang-client-0944935873`
- Firestore Database: `firestoredb`
- Region: `asia-northeast1`

### 4. 核心架构

- ✅ 前端: Next.js (Makerkit) + Firebase Hosting + Cloud Run
  - **4个核心页面**: `/dashboard`, `/offers`, `/adscenter`, `/tasks`
- ✅ 后端: Go 微服务 + API Gateway
  - **7个微服务API**: offers, siterank, batchopen, adscenter, billing, tasks, notifications
- ✅ 认证: Firebase Authentication (Google OAuth)
- ✅ 数据库: PostgreSQL + Firestore (自动同步)
- ✅ 实时更新: SWR 轮询 + Firestore Listener

**重要**: `/siterank` 和 `/batchopen` 只有后端API，无前端页面

---

## 🎯 您需要完成的配置（仅1项）

### Google Ads OAuth 配置

**⚠️ 已存在OAuth Client，仅需更新配置**

**请您在 Google Cloud Console 完成以下配置**:

1. 访问 [Credentials 页面](https://console.cloud.google.com/apis/credentials?project=gen-lang-client-0944935873)

2. 找到现有的 "Google Ads OAuth Client"
   - Client ID: `644672509127-sj0oe3shl7nltvn1agiuf1rv2vqgfsuj`

3. 编辑并更新 Authorized redirect URIs:
   ```
   https://www.urlchecker.dev/adscenter/callback
   https://www.autoads.dev/adscenter/callback
   http://localhost:3000/adscenter/callback (可选)
   ```

4. 确认 Authorized JavaScript origins:
   ```
   https://www.urlchecker.dev
   https://www.autoads.dev
   http://localhost:3000 (可选)
   ```

5. 前端环境变量配置:
   ```bash
   NEXT_PUBLIC_GOOGLE_ADS_CLIENT_ID=644672509127-sj0oe3shl7nltvn1agiuf1rv2vqgfsuj.apps.googleusercontent.com
   ```

**Client Secret已存储在Secret Manager**: `GOOGLE_ADS_OAUTH_CLIENT_SECRET`

**详细步骤**: 见 `07-前端环境配置指南.md` 第3节

---

## 🚀 开发路线图（15-20天）

### Phase 1: 环境搭建 ✅（1-2天）

**任务**:
- [x] 配置环境变量（`.env.local`）
- [ ] 创建 API 请求封装（`lib/api/client.ts`）
- [ ] 配置 SWR 全局设置
- [ ] 验证 Firebase Authentication
- [ ] 测试 API Gateway 连接

**产出**:
- API 请求工具可用
- 登录流程正常

---

### Phase 2: Offer 库页面 ⭐（3-5天）

**任务**:
- [ ] Offer 列表页面（TanStack Table）
- [ ] 添加 Offer 弹窗
- [ ] 评估功能集成
- [ ] 补点击配置弹窗
- [ ] 投放配置弹窗

**API 集成**:
- `GET /api/v1/offers` - 列表
- `POST /api/v1/offers` - 添加
- `POST /api/v1/siterank/evaluate` - 评估
- `POST /api/v1/batchopen/click-tasks` - 补点击
- `POST /api/v1/offers/{id}/deploy` - 投放

**产出**:
- `/dashboard/offers` 页面完整

---

### Phase 3: Ads 中心页面（2-3天）

**任务**:
- [ ] Ads 账号列表
- [ ] Google Ads OAuth 流程
- [ ] 账号同步功能
- [ ] 预算转移功能

**API 集成**:
- `GET /api/v1/adscenter/oauth/url` - 获取 OAuth URL
- `POST /api/v1/adscenter/oauth/callback` - 处理回调
- `GET /api/v1/adscenter/accounts` - 账号列表
- `POST /api/v1/adscenter/accounts/{id}/sync` - 同步

**产出**:
- `/dashboard/adscenter` 页面完整
- OAuth 授权流程可用

---

### Phase 4: 任务中心页面（2-3天）

**任务**:
- [ ] 任务列表页面
- [ ] 任务详情查看
- [ ] 任务进度查询（SWR 轮询）
- [ ] 任务取消功能

**API 集成**:
- `GET /api/v1/tasks` - 列表
- `GET /api/v1/tasks/{id}` - 详情
- `POST /api/v1/tasks/{id}/cancel` - 取消

**产出**:
- `/dashboard/tasks` 页面完整

---

### Phase 5: 大盘页面（2-3天）

**任务**:
- [ ] 大盘概览
- [ ] 核心指标卡片
- [ ] 趋势图表（recharts）
- [ ] 风险提醒卡片

**API 集成**:
- `GET /api/v1/dashboard/overview` - 概览
- `GET /api/v1/dashboard/trends` - 趋势

**产出**:
- `/dashboard` 页面完整

---

### Phase 6: 个人中心页面（2-3天）

**任务**:
- [ ] 个人资料页面
- [ ] 订阅管理页面
- [ ] Token 余额页面
- [ ] 签到功能

**API 集成**:
- `GET /api/v1/billing/subscription` - 订阅信息
- `GET /api/v1/billing/tokens` - Token 余额
- `POST /api/v1/billing/checkin` - 签到

**产出**:
- `/settings/*` 页面完整

---

### Phase 7: 优化和测试（1-2天）

**任务**:
- [ ] 通知系统（Toast）
- [ ] 错误处理
- [ ] 响应式适配
- [ ] 性能优化
- [ ] 端到端测试

**产出**:
- 完整的前端应用

---

## 📦 核心代码模板

### 1. API 请求封装

```typescript
// lib/api/client.ts
import { getAuth } from 'firebase/auth';

const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL;

export async function apiRequest<T>(
  endpoint: string,
  options?: RequestInit
): Promise<T> {
  const auth = getAuth();
  const token = await auth.currentUser?.getIdToken();

  const response = await fetch(`${API_BASE_URL}${endpoint}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`,
      ...options?.headers,
    },
  });

  if (!response.ok) {
    const error = await response.json();
    throw new APIError(error);
  }

  return response.json();
}
```

### 2. SWR Hook 示例

```typescript
// lib/hooks/useOffers.ts
import useSWR from 'swr';
import { apiRequest } from '~/lib/api/client';

export function useOffers(params?: {
  page?: number;
  limit?: number;
  status?: string;
}) {
  const queryString = new URLSearchParams(params).toString();
  const endpoint = `/offers?${queryString}`;

  return useSWR(endpoint, () => apiRequest(endpoint), {
    refreshInterval: 0, // 不自动刷新
    revalidateOnFocus: false,
  });
}
```

### 3. Firestore Listener 示例

```typescript
// lib/hooks/useOfferCache.ts
import { doc, onSnapshot } from 'firebase/firestore';
import { useEffect, useState } from 'react';
import { getFirestore } from '~/lib/firebase/firestore';

export function useOfferCache(offerId: string) {
  const [offer, setOffer] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (!offerId) return;

    const db = getFirestore();
    const offerRef = doc(db, `offers/${offerId}/cache`);

    const unsubscribe = onSnapshot(offerRef, (snapshot) => {
      if (snapshot.exists()) {
        setOffer(snapshot.data());
      }
      setLoading(false);
    });

    return () => unsubscribe();
  }, [offerId]);

  return { offer, loading };
}
```

---

## 🔧 开发环境设置

### 1. 安装依赖

```bash
cd apps/frontend
npm install
```

### 2. 配置环境变量

创建 `.env.local`:

```bash
# API Gateway
NEXT_PUBLIC_API_BASE_URL=https://autoads-gw-preview-885pd7lz.an.gateway.dev/api/v1

# Firebase (从 Secret Manager 获取)
NEXT_PUBLIC_FIREBASE_API_KEY=...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=...
NEXT_PUBLIC_FIREBASE_PROJECT_ID=gen-lang-client-0944935873
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=...
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OAuth
NEXT_PUBLIC_GOOGLE_ADS_OAUTH_REDIRECT_URI=http://localhost:3000/dashboard/adscenter/callback

# Feature Flags
NEXT_PUBLIC_ENABLE_WEBSOCKET=false
NEXT_PUBLIC_ENABLE_FILE_UPLOAD=false
```

### 3. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:3000`

---

## ✅ 开发前检查清单

在开始开发前，请确认：

### 必须完成
- [ ] Google Ads OAuth Client 已在 GCP Console 配置
- [ ] `.env.local` 文件已创建并配置正确
- [ ] Firebase 配置已从 Secret Manager 获取
- [ ] 开发服务器可正常启动

### 建议完成
- [ ] CORS 已在 API Gateway 配置（或 Go 微服务）
- [ ] PostgreSQL 触发器已部署
- [ ] Cloud Function (Firestore 同步) 已部署
- [ ] Firestore 安全规则已部署

### 可选
- [ ] API Gateway 测试通过（curl 测试）
- [ ] Firebase Authentication 测试通过
- [ ] 本地开发已连接 Firebase Emulator

---

## 📞 获取帮助

### 文档参考顺序

1. **快速了解**: `00-文档更新汇总.md`
2. **页面设计**: `01-页面设计方案.md`
3. **技术实现**: `02-技术实现方案.md`
4. **API 接口**: `05-API接口设计.md`
5. **环境配置**: `07-前端环境配置指南.md`

### 常见问题

**Q: API 请求返回 401 Unauthorized？**
A: 检查 Firebase ID Token 是否正确获取和传递

**Q: CORS 错误？**
A: 参考 `07-前端环境配置指南.md` 第4节配置 CORS

**Q: Firestore 数据不同步？**
A: 检查触发器和 Cloud Function 是否已部署

**Q: OAuth 回调失败？**
A: 确认 Redirect URI 已在 Google Cloud Console 配置

---

## 🎉 准备开始！

所有文档已就绪，配置已明确，您可以立即开始前端开发！

**建议开始顺序**:
1. ✅ 完成 Google Ads OAuth 配置
2. ✅ 启动 Phase 1: 环境搭建
3. ⭐ 开始 Phase 2: Offer 库页面（核心功能）

祝开发顺利！🚀

---

**文档版本**: v1.0
**最后更新**: 2024-10-03
**维护者**: AutoAds 开发团队
