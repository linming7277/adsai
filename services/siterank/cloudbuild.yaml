steps:
  - name: gcr.io/kaniko-project/executor:latest
    args:
      - --dockerfile=services/siterank/Dockerfile
      - --destination=asia-northeast1-docker.pkg.dev/gen-lang-client-0944935873/autoads-services/siterank:preview-latest
      - --context=.
      - --cache=true
      - --cache-ttl=6h
options:
  logging: LEGACY
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET
  sourceProvenanceHash: ['SHA256']
timeout: 1800s
